{{actor.login}} {{#isEqual payload.action 'closed'}}{{#if payload.pull_request.merged}}merged{{else}}closed{{/if}}{{else}}{{payload.action}}{{/isEqual}} pull request {{repo.name}}/#{{payload.number}} {{payload.pull_request.html_url}}